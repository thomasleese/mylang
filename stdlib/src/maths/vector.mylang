type exported Struct Vector {
	
	var exported Float x, y, z;
	
	def exported Float len2() {
		return this.x * this.x + this.y * this.y + this.z * this.z;
	}
	
	def exported Float len() {
		return sqrt(this.len2());
	}
	
}
